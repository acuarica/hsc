
let $len={\$xs->case $xs of {
   Nil->0;
   Cons $y $ys -> $plus 1 ($len $ys);
}} in
let $plus={\\$n->{\\$m->case $n of {
     0->$m;
     Succ $l -> $plus $l (Succ $m);
}}} in
  $len [1,2,3,4,5,6,7]
